#include <stdio.h>
#include <stdlib.h>

/**
    (fibo, fibo-inv) La suite de Fibonnacci est définie par U0 = 0, U1 = 1,
     et Un = Un−1 + Un−2 pour n ≥ 2.

     On s’intéresse à la valeur du n-ième terme de cette suite pour une valeur
     de n arbitrairement choisie par l’utilisateur et inférieure à 100.

    1. Écrire un programme fibo qui demande à l’utilisateur une valeur pour n,
       puis affiche les valeurs k et Uk pour k variant de 0 à n.
       Observer les résultats obtenus selon que les valeurs calculées soient
       en int,long,long long et float.

    2. Écrire un programme fibo-inv qui demande à l’utilisateur une valeur pour n,
       puis affiche les valeurs k et Uk pour k variant de n à 0,
       c’est-à-dire qui affiche les n premières valeurs de la suite
       de Fibonnacci en ordre inverse de celui du calcul.
*/

int main()
{

    return 0;
}
