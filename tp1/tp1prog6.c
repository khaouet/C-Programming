#include <stdio.h>
#include <stdlib.h>

/**
      Dans cet exercice les affichages seront systématiquement réalisés en décimal,
      en hexadécimal (resp. %d,%x) et en binaire.
      Pour i = −2, . . . , 5, calculer et afficher des décallages de i positions
      à droite et à gauche de valeurs arbitraires entrées au clavier.
      Interpréter le sens de x << 2, ou plus généralement de x << i, pour i ≥ 0.
      Commenter les résultats obtenus dans le cas des positions négatives (i < 0).


*/


int main()
{

    return 0;
}
